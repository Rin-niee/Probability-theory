import math
# def factor(f):
#     if f == 0:
#         return 1
#     if f == 1:
#         return 1
#     else:
#         return factor(f-1)*f

def sochet(k, k1):
    o = math.factorial(k1)/(math.factorial(k) * math.factorial(k1-k))
    return o
    
#1 t,fyysq rjl

x = int(input("Введите выбранные виды"))
p1= int(input("Введите количество видов"))
p12= p1-x
p = [] #список, куда заносятся данные итоговые(x)
x12 = x #выбранные виды
p0=0
for i in range(x+1):
    p0 = sochet(i, x)*sochet(x12, p12)/sochet(x, p1)
    p.append(p0)
    x12=x12-1
    if x12<0:
        break
    
print(p)

#2  Радист вызывает корреспондента, причем каждый последующий вызов производится лишь в том случае, если предыдущий вызов не принят. 
# Вероятность того, что корреспондент примет вызов, равна 0,4. 
# Составить закон распределения числа вызовов, если число вызовов не более 5.

x2 = int(input("Введите количество вызовов"))
p2= float(input("Введите вероять принятия вызова"))
p21 = 1-p2
x22=0
while x22<x2:
    t2 = p21**x22 * p2
    x22+=1
    
    if x22==x2:
        t2 = p21**x22 * p2 * p21**x2
    print(t2)


#3 Вероятности того, что студент сдаст семестровый экзамен 
# в сессию по дисциплинам А и Б, равны соответственно 0,7 и 0,9. 
# Составить ряд распределения числа семестровых экзаменов, которые сдаст студент

x3 = int(input("Введите количество экзаменов"))
p31=float(input("Введите вероятность сдачи первого экзамена")) #0.7
p32= float(input("Введите вероятность сдачи второго экзамена")) #0.9


P30=(1-p31)*(1-p32) 
P31=p31*((1-p32)+ (1-p31)) * 0.9 
P32=p31*p32 
print(P30, P31, P32)

#4 Z = 2X+3Y

#7
# Два  консервных  завода  поставляют  продукцию  в  магазин  в  пропорции  2:3.  
# Доля продукции высшего качества на первом заводе составляет 90%, а на втором - 80%. В  
# магазине  куплено  3  банки  консервов. 
# Составить ряд распределения случайной величины  X - число банок с  продукцией  
# высшего  качества  среди  купленных  трех  банок. 


# A - банка высшего качества 

H1 = int(input("введите количество банок с первого завода"))

H2 = int(input("введите количество банок со второго  завода"))
H = H1+H2

PAH1= int(input("введите процент продуктов высшего качества на 1 заводе"))/100
PAH2 = int(input("введите процент продуктов высшего качества на 2 заводе"))/100
Hehe= int(input("Введите количество купленных банок"))

PA = (H1/H*PAH1)+(H2/H * PAH2)
haha=0
aaa= Hehe
p3 = []
p31=0
for i in range(Hehe):
    pe = sochet(p31, Hehe)*PA**i * (1-PA)**aaa
    aaa-=1
    p31+=1
    p3.append(pe)
    if p31<0:
        break
print(p3)
